{
  "name": "generative-city-wallet",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered city wallet",
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "smoke-test": "bash scripts/smoke-test.sh"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
